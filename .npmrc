# Force the public npm registry for all environments (including Vercel CI).
# Replit injects its own registry at runtime via a separate mechanism, so
# this file only takes effect when the Replit runtime does NOT override it
# (e.g. on Vercel, GitHub Actions, local machines).
registry=https://registry.npmjs.org
