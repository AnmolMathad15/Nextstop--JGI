Upgrade the existing project "NextStop JGI" into an enterprise-grade real-time campus transportation system exclusively for JCET.

IMPORTANT:
Do NOT remove the existing Route → Stop → Map workflow.
Preserve the 6 predefined routes exactly as they are.
Do NOT regenerate from scratch. Modify incrementally.

------------------------------------
🎯 GOAL
------------------------------------
Make the system production-ready, secure, structured, and intelligent — but single-tenant (JCET only).

------------------------------------
🧱 1. DATABASE STRUCTURE (Simplified Enterprise)
------------------------------------

Admin
- id (PK)
- name
- email
- password_hash
- role (ADMIN)

Route
- id (PK)
- name
- display_order
- is_active

Stop
- id (PK)
- route_id (FK)
- name
- latitude
- longitude
- stop_order

Bus
- id (PK)
- route_id (FK)
- bus_number
- current_latitude
- current_longitude
- speed
- is_active

Driver
- id (PK)
- bus_id (FK)
- name
- phone
- password_hash
- is_active

Student
- id (PK)
- route_id (FK)
- name
- usn
- email
- password_hash

SpeedViolationLog
- id (PK)
- bus_id (FK)
- speed
- timestamp

DelayLog
- id (PK)
- route_id (FK)
- delay_minutes
- timestamp

------------------------------------
🔐 2. SECURITY IMPLEMENTATION
------------------------------------

- JWT authentication
- bcrypt password hashing
- Role-based middleware:
    studentOnly
    driverOnly
    adminOnly

Secure all API routes.
Validate JWT for WebSocket subscription.

------------------------------------
📡 3. REAL-TIME ENGINE UPGRADE
------------------------------------

Improve WebSocket:

- route_<route_id> channels
- Auto-reconnect
- Heartbeat ping every 20 seconds
- Ignore invalid GPS coordinates

Driver sends location.
Server calculates ETA.
Server broadcasts to subscribed students.

------------------------------------
🧠 4. ETA ENGINE
------------------------------------

Server-side ETA calculation:

ETA = distance(current_location, stop_location) / average(last_5_speed_values)

Send ETA with each update.

------------------------------------
⚠ 5. INTELLIGENCE FEATURES
------------------------------------

Speed violation:
IF speed > 60 km/h
→ Insert into SpeedViolationLog

Route deviation:
IF bus deviates from assigned route polyline
→ Flag as "Off Route"

------------------------------------
📊 6. ADMIN ANALYTICS DASHBOARD
------------------------------------

Create endpoint:
GET /admin/analytics

Return:
- Average delay per route
- Speed violations count
- On-time performance %
- Most used route

------------------------------------
🛡 7. SECURITY HARDENING
------------------------------------

- express-validator
- rate limiting
- helmet
- CORS control
- environment validation

------------------------------------
📁 8. PROJECT STRUCTURE CLEANUP
------------------------------------

server/
  controllers/
  routes/
  middleware/
  services/
  websocket/
  utils/

Separate:
- Auth logic
- ETA logic
- Speed logic

------------------------------------
🎨 9. FRONTEND IMPROVEMENT
------------------------------------

- Professional admin dashboard
- Sidebar navigation
- Role-based views
- Real-time ETA badge
- Animated bus marker
- Dark mode toggle
- Clean minimal design

------------------------------------
🎯 FINAL RESULT
------------------------------------

A secure, intelligent, enterprise-grade real-time transportation system for JCET.