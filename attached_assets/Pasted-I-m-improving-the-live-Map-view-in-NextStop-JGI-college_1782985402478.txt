I'm improving the live Map view in NextStop JGI (college bus tracking app). 
Stack: React 18 + TypeScript, Leaflet + React-Leaflet, Tailwind + shadcn/ui, 
TanStack Query, WebSocket for live GPS, Framer Motion, Wouter.

Currently the map shows the full default OpenStreetMap tile layer 
(all roads, all city labels, all POIs) with only 2 generic markers. I want 
a clean, professional tracking-app UI (like Ola/Uber/IRCTC live train 
tracking) that shows ONLY the relevant route, not the entire city.

Implement:

1. BASE MAP STYLE
   - Replace the default OSM tile layer with CartoDB Positron (light, 
     minimal, desaturated) tiles: 
     https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png
   - Reduce zoom-level label clutter — Positron already does this, but 
     confirm attribution is added per CartoDB's usage requirements.

2. ROUTE-SCOPED RENDERING
   - When a route is selected (from the Routes page), the Map view should 
     only render markers/polyline for THAT route's stops — not all routes, 
     not all city data.
   - Fetch the selected route's stops (ordered list of lat/lng + names) 
     from the existing route/stop schema.
   - Draw a Polyline (React-Leaflet <Polyline>) connecting the stops in 
     order, styled with the brand accent color (teal), weight 4, 
     rounded caps.
   - Auto-fit the map viewport to the route's bounding box on load using 
     map.fitBounds() with ~50px padding — don't leave it zoomed out to 
     the whole city.

3. MARKER HIERARCHY — create distinct custom Leaflet divIcon markers for:
   a) Main stops — larger circular marker, teal fill, white border
   b) Sub-stops — smaller dot marker, muted gray/teal
   c) Live bus position — bus icon (Lucide Bus icon rendered as divIcon), 
      with a Framer Motion-driven pulsing ring animation, subscribed to 
      live WebSocket position updates, positioned above other markers 
      (highest z-index)
   d) Destination (college) — distinct flag/building pin, fixed position

4. REMOVE CLUTTER
   - Do not show POI labels, unrelated road names, or markers from other 
     routes when a specific route is active.
   - If no route is selected yet, show a simplified default view centered 
     on the college with a prompt to select a route (reuse existing route 
     selection UI/modal).

5. STOP LIST + MAP SYNC (bottom sheet)
   - Add a collapsible bottom sheet/drawer (shadcn Sheet or a custom 
     Framer Motion drawer) listing this route's stops in order with ETA 
     per stop (if available).
   - Tapping a stop in the list should pan/fly the map to that marker 
     (map.flyTo) and highlight it.
   - Keep the existing "Search destination" bar functional — filter the 
     stop list and highlight matching markers on the map as the user types.

6. LIVE BUS TRACKING
   - Live bus marker position updates via existing WebSocket connection — 
     animate marker movement smoothly between GPS pings (interpolate 
     position over ~2-3s using requestAnimationFrame or Framer Motion, 
     rather than snapping instantly) so it feels like real-time tracking, 
     not choppy updates.

Keep existing bottom nav, header, and route-selection flow unchanged — 
only redesign the Map view's rendering and add the bottom sheet stop list.

Show me:
1. Updated Map page component
2. Custom marker components (StopMarker, BusMarker, DestinationMarker)
3. The bottom sheet stop list component
4. Any shared types needed for route/stop/live-position data