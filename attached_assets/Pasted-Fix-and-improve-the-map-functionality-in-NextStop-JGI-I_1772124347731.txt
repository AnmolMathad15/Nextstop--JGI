Fix and improve the map functionality in NextStop JGI.

IMPORTANT:
Remove any shortest-path routing logic (OSRM or dynamic routing).
This is a fixed campus bus system with predefined stops.
Do NOT redesign the system.
Do NOT break Route → Stop → Map workflow.
Only improve map behavior.

---------------------------------------
GOAL
---------------------------------------
1. Show user's current location on the map (blue dot).
2. Display predefined fixed route polyline.
3. Show selected stop marker clearly.
4. Show live bus marker.
5. Calculate ETA based on stop order (not shortest road).
6. Clean and professional UI.
7. Remove any dynamic shortest path API calls.

---------------------------------------
STEP 1: CURRENT LOCATION
---------------------------------------
- Use navigator.geolocation.
- Ask permission properly.
- Center map to user location.
- Add blue circular marker.
- Handle permission denial gracefully.

---------------------------------------
STEP 2: FIXED ROUTE DISPLAY
---------------------------------------
- Fetch all stops of selected route.
- Sort by stop_order.
- Create polyline using those stop coordinates.
- Draw clean single polyline (teal color, weight 5).
- Remove old route before drawing new one.
- Prevent multiple polylines.

---------------------------------------
STEP 3: LIVE BUS MARKER
---------------------------------------
- Use WebSocket to update bus location.
- Animate marker movement smoothly.
- Do not redraw entire map on update.
- Only move bus marker.

---------------------------------------
STEP 4: ETA CALCULATION (STOP ORDER BASED)
---------------------------------------
ETA logic:
1. Find current nearest stop index.
2. Find selected stop index.
3. If bus already passed → show "Bus Already Crossed".
4. Otherwise:
   ETA = sum of distances between intermediate stops / current_speed

Do not use shortest-path routing.

---------------------------------------
STEP 5: MAP CLEANUP RULES
---------------------------------------
- Only one active route polyline.
- Only one bus marker.
- Only one user marker.
- Fit map bounds to route on first load.
- Do not zoom excessively on every update.
- Keep UI minimal and professional.

---------------------------------------
EXPECTED RESULT
---------------------------------------
Student selects route → 
Map shows:
• Fixed predefined route
• Live bus moving
• Student current location
• Selected stop highlighted
• ETA shown based on route order
• No dynamic routing

Apply changes incrementally.
Preserve working backend logic.
Only improve map-related logic.