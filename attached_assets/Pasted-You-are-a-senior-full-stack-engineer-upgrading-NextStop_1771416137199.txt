You are a senior full-stack engineer upgrading "NextStop JGI" into a production-grade SaaS fleet tracking system.

STRICT RULES:
- Do NOT modify UI layout.
- Preserve overlays and search UI.
- Only upgrade tracking, analytics, and backend intelligence.
- Keep WebSocket architecture intact.
- Do not regenerate project.

OBJECTIVE:
Implement a complete fleet intelligence system including:

========================
PART 1 — MAP ENGINE (Frontend)
========================

1. Replace Leaflet with Mapbox GL JS.
2. Initialize map once using useRef.
3. Use vector style.
4. Add navigation control.
5. Create bus marker once.
6. Rotate marker using heading.
7. Animate movement smoothly over 3 seconds using requestAnimationFrame.
8. Use easeTo for smooth camera follow.
9. Prevent React re-render from moving marker.
10. Avoid memory leaks.

========================
PART 2 — ROAD SNAPPING
========================

1. Integrate Mapbox Map Matching API.
2. Send previous + current coordinates.
3. Throttle to once every 3 seconds.
4. Receive snapped GeoJSON geometry.
5. Animate along snapped path.
6. Fallback to raw GPS if API fails.

========================
PART 3 — SPEED INTELLIGENCE (Backend)
========================

1. Use Haversine formula.
2. Calculate speed = distance / time.
3. Convert to km/h.
4. Store in database.
5. Detect overspeed > 60 km/h.
6. Increment driver overspeed count.
7. Maintain driver score:
   score = 100 - (overspeed_count * 5)

8. Ignore GPS jumps > 300 meters.
9. Ignore updates < 3 seconds apart.

========================
PART 4 — GEO-FENCING SYSTEM
========================

1. Add radius field to route stops (default 300m).
2. On every location update:
   - Calculate distance to stop.
   - If entering radius → emit ENTER event.
   - If leaving radius → emit EXIT event.
3. Store geo_events in database.
4. Prevent duplicate ENTER/EXIT events.
5. Compute arrival delay vs scheduled time.

========================
PART 5 — ETA ENGINE
========================

1. Use snapped path distance.
2. Use speed if available.
3. Estimate realistic ETA.
4. Update in WebSocket broadcast.

========================
PART 6 — PERFORMANCE
========================

1. Avoid full map re-render.
2. Avoid repeated marker recreation.
3. Prevent overlapping animations.
4. Clean WebSocket listeners on unmount.
5. Use refs instead of React state for animation.

Return only modified files.
Explain changes briefly.
