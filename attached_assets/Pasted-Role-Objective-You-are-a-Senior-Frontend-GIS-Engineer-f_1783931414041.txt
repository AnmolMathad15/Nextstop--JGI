Role & Objective:

You are a Senior Frontend GIS Engineer fixing a critical rendering bug in our "NextStop JGI" college bus tracking application. We migrated from MapLibre GL JS to Mapbox GL JS, but the app is throwing the following console error and rendering a blank screen: Map cannot fit within canvas with the given bounds, padding, and/or offset.

Your objective is to fix this DOM timing/canvas sizing bug, guarantee a visible Mapbox render, completely purge any remaining MapLibre code, and implement our real-time bus tracking layers.

Phase 1: Fix the Canvas Sizing Bug (Mandatory CSS & Layout Rules)

The Map cannot fit within canvas error occurs because Mapbox is initializing inside a wrapper with 0px height or before flex/grid layouts finish calculating bounds. You must enforce these strict layout rules:

Explicit Parent Sizing: Find the component wrapping the map container. Ensure the parent <div className="map-wrapper"> (or similar) has explicit CSS: display: flex; flex: 1; width: 100%; min-height: 600px; height: 100vh; position: relative;.

Map Canvas Container Sizing: The DOM element passed to container: 'map' MUST have: width: '100%', height: '100%', position: 'absolute', top: 0, left: 0. Do not let it default to auto or 0px.

Purge MapLibre: Ensure zero imports, dependencies (package.json), or script tags for maplibre-gl remain anywhere in the project.

Phase 2: Safe Mapbox Initialization & Lifecycle Timing

Setup Constants: Declare placeholders at the top of the map module:

const MAPBOX_TOKEN = 'YOUR_MAPBOX_ACCESS_TOKEN_HERE';

const CUSTOM_STYLE = 'mapbox://styles/YOUR_USERNAME/YOUR_CUSTOM_STYLE_ID';

const FALLBACK_STYLE = 'mapbox://styles/mapbox/streets-v12';

Avoid Bounding Conflicts: Do NOT call map.fitBounds() or maxBounds with padding during initial rendering. Initialize cleanly with simple standard coordinates: center: [75.1240, 15.3647], zoom: 13, pitch: 45.

Mandatory Resize Trigger: To prevent Replit's iframe and Vite layout shifts from breaking the canvas, you MUST add a resize recalculation inside the load event:

JavaScript



map.on('load', () => {

  map.resize(); // FORCES canvas dimensions to recalculate to match CSS

  console.log('Map loaded and canvas resized successfully!');

});

Window Resize Handler: Attach a window resize listener to ensure the canvas adapts if the Replit workspace panel is dragged: window.addEventListener('resize', () => map.resize()); (ensure this is cleaned up on component unmount if using React).

Phase 3: Real-Time Bus Tracking Architecture

Once map.on('load') fires (and after calling map.resize()), initialize the live tracking layers:

Route Line: Add a GeoJSON LineString source named bus-route with a placeholder array of route coordinates. Render it using a line layer styled with #3b82f6 (primary blue), 5px width, and rounded joints/caps.

High-Performance Live Markers: Do NOT destroy and recreate DOM elements on GPS telemetry updates.

Add a GeoJSON source named live-bus-source initialized with a Point feature at [75.1240, 15.3647].

Add a styled circle or icon layer tied to live-bus-source (e.g., #ef4444 red with a white stroke).

State Update Function: Create and export updateBusPosition(lng, lat, speed, busId). When invoked by websocket/backend updates:

Dynamically update the vehicle coordinates using map.getSource('live-bus-source').setData(...).

Use map.panTo([lng, lat], { duration: 1000 }) to smoothly follow the vehicle.

Interactive Popup: Attach a click listener to the bus layer that opens a mapboxgl.Popup showing the Bus ID, Current Speed, and Last Updated status.

Phase 4: Immediate Visual Verification

Create an auto-executing function called startSimulatedTracking() that runs once the map finishes loading. It should shift the bus marker coordinates slightly every 3 seconds along a predefined path and log "Simulating GPS Telemetry Update..." to the console so we can visually confirm the map is rendering and markers are animating immediately upon code genera